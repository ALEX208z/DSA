Binary Search

Sorted array or O(log n)-time complexity → binary search
- non-decreasing order means there can be duplicates


correct = index + 1
missing = nums[value] - (inex + 1)

Integer ceiling (a + b - 1)/b instead of Math.ceil((double)a/b)